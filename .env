# Disable source maps to speed up builds and avoid timeout errors
GENERATE_SOURCEMAP=false

# Disable opening browser automatically on start
BROWSER=none

# Set build optimization
INLINE_RUNTIME_CHUNK=false

# Disable ESLint warnings during build (optional)
DISABLE_ESLINT_PLUGIN=false

# Set development server port (optional)
PORT=3000
